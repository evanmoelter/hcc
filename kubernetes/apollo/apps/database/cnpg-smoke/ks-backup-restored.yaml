# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cnpg-smoke-backup-restored
  namespace: flux-system
spec:
  dependsOn:
    - name: cnpg-smoke-verify
  targetNamespace: database
  path: ./kubernetes/apollo/apps/database/cnpg-smoke/backup-restored
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
  wait: true
  healthCheckExprs:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Backup
      failed: has(status.phase) && status.phase == 'failed'
      current: has(status.phase) && status.phase == 'completed'
  interval: 30m
  retryInterval: 1m
  timeout: 30m
